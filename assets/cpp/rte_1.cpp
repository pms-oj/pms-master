#include <bits/stdc++.h>

using namespace std;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t = 0;
    cin >> t;
    while (t--) {
        int a = 0, b = 0;
        cin >> a >> b;
        cout << a+b << "\n";
    }
    // exit code: 1 (NZEC)
    return 1;
}